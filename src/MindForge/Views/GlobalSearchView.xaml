<UserControl x:Class="MindForge.Views.GlobalSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="🔍 Globale Suche" Foreground="{DynamicResource TextBrush}"
                       FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Suche in Notizbüchern, Materialien und Chat-Verläufen"
                       Foreground="{DynamicResource TextMutedBrush}" FontSize="14" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search bar -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch"
                     materialDesign:HintAssist.Hint="Suchbegriff eingeben…"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     Height="48" FontSize="15"
                     KeyDown="OnSearchKeyDown"/>
            <Button Grid.Column="1" Content="🔍 Suchen" x:Name="BtnSearch"
                    Click="OnSearch"
                    Background="{DynamicResource AccentBrush}" Foreground="White"
                    BorderThickness="0" Height="48" Padding="20,0" Margin="12,0,0,0"
                    FontSize="14"/>
            <Button Grid.Column="2" Content="🔄 Index aufbauen" x:Name="BtnRebuild"
                    Click="OnRebuildIndex"
                    Background="{DynamicResource BgTertiaryBrush}"
                    Foreground="{DynamicResource TextMutedBrush}"
                    BorderThickness="0" Height="48" Padding="16,0" Margin="8,0,0,0"
                    FontSize="13" ToolTip="Suchindex neu aufbauen"/>
        </Grid>

        <!-- Results area -->
        <Grid Grid.Row="2">
            <!-- Empty / loading state -->
            <TextBlock x:Name="TxtStatus" Text="Suche nach einem Begriff, um Ergebnisse zu sehen."
                       Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Visible"/>

            <!-- Results list -->
            <ScrollViewer x:Name="ResultsScroll" VerticalScrollBarVisibility="Auto"
                          Visibility="Collapsed">
                <ItemsControl x:Name="ResultsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource BgSecondaryBrush}"
                                    CornerRadius="10" Padding="20" Margin="0,0,0,10"
                                    Cursor="Hand" MouseLeftButtonDown="OnResultClick"
                                    Tag="{Binding}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource BgTertiaryBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Icon}"
                                               FontSize="28" VerticalAlignment="Center"
                                               HorizontalAlignment="Center"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                        <TextBlock Text="{Binding Title}"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontSize="15" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Snippet}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   FontSize="12" TextWrapping="Wrap"
                                                   MaxHeight="40" Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <Border Grid.Column="2" Background="{DynamicResource BgBrush}"
                                            CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding EntityType}"
                                                   Foreground="{DynamicResource AccentBrush}"
                                                   FontSize="11"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
